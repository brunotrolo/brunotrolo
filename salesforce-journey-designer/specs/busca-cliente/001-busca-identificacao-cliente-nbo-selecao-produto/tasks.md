# Tasks — `busca-cliente/001` — Busca e identificação de cliente com NBO e seleção de produto

> **Ordem de build real:** dados + segurança → integração/automação Apex → UI LWC → migração progressiva → testes → cutover.
> **Validação UX:** `prototype/` (LWC real sobre SLDS2 real) **validado e aprovado 2026-09-06 (gate VIII)** — não re-litigar UX, apenas realizar tecnicamente. Ver `prototype/README.md`.
> **Premissas abertas:** 17× [NEEDS CLARIFICATION] mantidas como placeholder neutro (ver `plan.md` §5/§17; item 11 removido com o histórico) — nenhuma regra inventada.
> **Princípios:** VI (padrão primeiro, sem objeto customizado de domínio), VII (segurança re-derivada), IX (cada cenário tem task).

---

## Grupo A — Modelo de dados (Princípio VI: sem objeto customizado novo, só campos em padrão core)

Dependência: nenhum — base para todos os demais. Bloqueia B/C/D/E/F.

- [ ] **T-A01 — Account.Document__c (External ID)** — Criar campo `Account.Document__c` (Text 18, External ID, Unique, Indexed) + `Account.DocumentType__c` (Picklist `CPF`/`CNPJ`/`CNPJ_ALFANUM`) + `Account.CustomerSegment__c` (Picklist: `VAREJO`/`ALTA_RENDA`/`CORPORATE`/`EXCLUSIVO`) — **Depende de:** nada — **Cenários:** 1–6, EL-01/02 — **Aceite:** `Database.upsert` por `Document__c` não duplica; `Account_Document_Required` validation rule ativa.
- [ ] **T-A02 — Contact.Document__c mirror** — Criar `Contact.Document__c` (Text 18, não unique) para titular PF espelhar `Account.Document__c` — **Depende de:** T-A01 — **Cenários:** 6 — **Aceite:** `Contact` inserido com mesmo documento do `Account` pai.
- [ ] **T-A03 — Asset campos de contrato/via/cota/posição + Holding** — Criar em `Asset`: `ExternalContractId__c` (Text 36, External ID, Unique), `ProductType__c` (Picklist global: `CARTAO`/`CONTA_DIGITAL`/`CONSORCIO`/`INVESTIMENTOS`/`HOLDING_AUTO`/`HOLDING_RESIDENCIAL`/`HOLDING_SAUDE`), `IsHoldingInformative__c` (Checkbox), `ViaStatus__c` (Picklist `Ativo`/`Cancelado`/`Bloqueado`), `Last4__c` (Text 4), `CardBrand__c` (Text 20), `CardCategory__c` (Text 20), `HolderName__c` (Text 80), `IsTitular__c` (Checkbox), `ConsorcioTipo__c`, `ConsorcioGrupo__c`, `ConsorcioCota__c`, `ValorContrato__c` (Currency), `Contemplacao__c`, `InvestClasse__c`, `InvestProduto__c`, `Rentabilidade__c`, `Vencimento__c`, `Risco__c` (Picklist Baixo/Moderado/Alto) — **Depende de:** T-A01 — **Cenários:** 7,8,17–20, EL-03/04/05 — **Aceite:** `Asset.ParentId` hierarquia (pai contrato → filhos vias/cotas/posições) funciona; `Asset_Last4_Format` validation `REGEX(Last4__c,"\\d{4}")`.
- [ ] **T-A04 — Opportunity campos NBO** — Criar em `Opportunity`: `ExternalOfferId__c` (Text 36, External ID, Unique), `NboProductType__c` (Text 40), `NboBenefit__c` (Long Text 1000), `NboStatus__c` (Picklist `Elegível`/`Abordada`/`Dispensada`/`Expirada`), `ApproachTimestamp__c` (DateTime), `DismissalReason__c` (Text 255), `LastNboInteractionBy__c` (Lookup User), `IsPrimaryNbo__c` (Checkbox), `NboSuppressedUntil__c` (DateTime) — **Depende de:** T-A01 — **Cenários:** 11–15 — **Aceite:** Stage `Elegível` → `Abordada`/`Dispensada` atualiza `ApproachTimestamp__c` sem recriar `Opportunity`.
- [ ] **T-A05 — Product2 catálogo base** — Criar 7 `Product2` ativos: `CARTAO`, `CONTA_DIGITAL`, `CONSORCIO`, `INVESTIMENTOS`, `HOLDING_AUTO`, `HOLDING_RESIDENCIAL`, `HOLDING_SAUDE` (`ProductCode` = `ProductType__c`, `IsActive=true`) — **Depende de:** T-A03/T-A04 — **Cenários:** 7, PT 23 — **Aceite:** `SELECT Id FROM Product2 WHERE ProductCode='CARTAO'` retorna 1 ativo; `Asset.Product2Id` aponta para ele.
- [ ] **T-A06 — ProductCatalog__mdt (único CMDT novo)** — Criar Custom Metadata Type `ProductCatalog__mdt` com campos `ProductType__c` (Text 40, unique), `Label__c` (Text 80), `IconName__c` (Text 40), `RequiresViaSelection__c` (Checkbox), `DisplayOrder__c` (Number 2.0), `IsActive__c` (Checkbox), `HoldingInformative__c` (Checkbox) + 7 registros (CARTAO `RequiresViaSelection=true, order 1`; CONTA `false,2`; CONSORCIO `true,3`; INVESTIMENTOS `true,4`; HOLDING_* `HoldingInformative=true`) — **Depende de:** T-A05 — **Cenários:** PT 23/RN-12 — **Aceite:** `ProductCatalog__mdt.getAll().size()==7`; `c-product-hub` renderiza ordem `DisplayOrder__c` sem código.
- [ ] **T-A07 — Validation Rules + Hierarchy Setting base** — Criar `Account_Document_Required` ( `ISBLANK(Document__c)` ), `Account_Document_Unique` (via External ID), `Asset_Last4_Format`, + `Hierarchy Custom Setting TriggerBypass__c` (Checkbox `Asset__c`, `Opportunity__c` default `true` para esta capacidade) — **Depende de:** T-A01/T-A03 — **Cenários:** EL-01/02 — **Aceite:** insert `Account` sem `Document__c` falha; `TriggerBypass__c.Asset__c=true` desabilita handler.

## Grupo B — Segurança (Princípio VII: re-derivado, não copiado)

Dependência: A (objetos/campos existem). Bloqueia C/D.

- [ ] **T-B01 — OWD Private re-derivado** — Configurar OWD (via `Security Settings`): `Account=Private`, `Contact=ControlledByParent`, `Asset=Private` (ou `ControlledByParent` se `Asset` suportar), `Opportunity=Private`, `Product2=Public Read Only` — documentar em `plan.md` §13.1 como premissa aberta LGPD — **Depende de:** T-A01 — **Cenários:** premissas 01/02 — **Aceite:** `Setup → Sharing Settings` mostra valores acima; sem `SharingRule` nesta capacidade.
- [ ] **T-B02 — Permission Set PS_BuscaCliente_Agente** — Criar `PS_BuscaCliente_Agente` (label `Busca Cliente — Agente Porto Bank`): `Account` Read (todos campos + `Document__c`/`CustomerSegment__c`), `Contact` Read, `Asset` Read (todos `ProductType__c` + customs), `Opportunity` Read/Create/Edit (`NboStatus__c` etc), `ProductCatalog__mdt` Read, Apex `CustomerSearchController`, `CadastroService`, `NboService`, `CardService` executáveis, LWCs visíveis — **Depende de:** T-B01, T-A07 — **Cenários:** todos (acesso) — **Aceite:** usuário com só este PS consegue `identify()` e vê hub; sem PS falha com `INSUFFICIENT_ACCESS`.
- [ ] **T-B03 — Permission Set PS_BuscaCliente_Admin + FLS Holding restrito** — Criar `PS_BuscaCliente_Admin` (CRUD `Account/Asset/Opportunity` + `Customize Application` p/ `ProductCatalog__mdt` + `TriggerBypass__c` edit) e preparar `PS_BuscaCliente_HoldingRestrito` (placeholder premissa 02: `Asset` Read só `ProductType__c`/`ViaStatus__c`, sem `Last4__c`/`HolderName__c` via FLS) — **Depende de:** T-B02 — **Cenários:** premissa 02 — **Aceite:** FLS de `HoldingRestrito` oculta `Last4__c`; Admin consegue editar `ProductCatalog__mdt`.

## Grupo C — Integração e Automação Apex (sem IP, Service Layer + Queueable)

Dependência: A+B (dados+segurança). Bloqueia D (UI consome Apex).

- [ ] **T-C01 — Named Credentials externas** — Criar 5 `NamedCredential` + `ExternalCredential` (OAuth2/JWT conforme barramento): `NC_CadastroCorporativo` (`callout:NC_CadastroCorporativo/api/v2/clientes/`), `NC_NboMotor` (`/api/v1/nbo/elegibilidade/`), `NC_CartoesProcessadora` (`/api/v1/cartoes/`), `NC_Consorcio` (`/api/v1/consorcios/`), `NC_Investimentos` (`/api/v1/investimentos/`) — `Generate Authorization Header=true`, `PrincipalType=NamedPrincipal`, timeouts configurados no Apex (2.5s/3.0s) — **Depende de:** T-B02 — **Cenários:** I01–I03c — **Aceite:** `AuthProvider` + `NamedCredential` testado via `HttpCalloutMock` sem hardcode de endpoint no Apex.
- [ ] **T-C02 — HttpCalloutService (infra timeout/DTO fault)** — Classe `HttpCalloutService` (método `HttpResponse call(NamedCredential nc, String path, Integer timeoutMs)` → `HttpResponse` ou `ApiFaultDTO {source, code='TIMEOUT'|'HTTP_500', message}`) com `setTimeout` por serviço (I01 2500ms, I02/I03/I03b/I03c 3000ms) — **Depende de:** T-C01 — **Cenários:** 21,22, EL-08/09 — **Aceite:** `TimeoutException` → `ApiFaultDTO` não lança exceção para LWC; `Promise.allSettled` no LWC recebe partial.
- [ ] **T-C03 — DocumentValidator** — Classe `DocumentValidator` (`validate(String normalizedDoc, String modo) → {docType, valid, message}`, `validateCPF`, `validateCNPJ`, `validateCNPJAlfa` formato `^[A-Z0-9]{12}\\d{2}$` sem DV alfanumérico — premissa 07, `validateProtocolo` 5+ chars, `validateNaoCliente` pass-through) — **Depende de:** nada (puro) — **Cenários:** 1–4, EL-01/02 — **Aceite:** `validate('12345678909','PF')=CPF válido`, `12ABC34501DE35=alfa válido`, `12345678999=DV inválido`, `validate('12345','PROTOCOLO')=válido` conforme `prototype/data/buscaCliente/buscaCliente.js`.
- [ ] **T-C04 — DTOs Apex tipados** — Classes `CustomerSearchResultDTO`, `DemographicDTO`, `ProductSummaryDTO`, `HoldingDTO`, `NboOfferDTO` (com `titulo`, `narrativa`, `bandeiras`, `limites`, `anuidade`, `beneficios`), `CardViaDTO` (com `limite`, `anuidade`), `ConsorcioCotaDTO` (tipo, grupo, cota, valor, contemplação, parcela, status), `InvestimentoPosicaoDTO` (classe, produto, valor, rentabilidade, vencimento, risco), `InteractionContextDTO` (com `viaAssetId`, `cotaAssetId`, `investimentoAssetId`), `ApiFaultDTO` (todos `@AuraEnabled`) — **Depende de:** T-C03 — **Cenários:** todos — **Aceite:** `JSON.serialize(DTO)` round-trip sem `FinServ__*`; `Last4` exposto como `last4Masked='****1234'`.
- [ ] **T-C05 — CadastroService + HoldingService** — `CadastroService.fetch(String doc)→DemographicDTO+PortoBankWallet+holding` e `HoldingService` (ou branch) parseando `GET NC_CadastroCorporativo/{doc}` JSON → `ProductSummaryDTO`/`HoldingDTO` filtrando `origem` — **Depende de:** T-C02, T-C04 — **Cenários:** 6,7,8, EL-12 — **Aceite:** mock I01 com 4 Porto Bank + 6 Holding → `portoBankWallet.size()==4`.
- [ ] **T-C06 — NboService + NboInteractionService** — `NboService.fetch(doc)→List<NboOfferDTO>` (`GET NC_NboMotor/{doc}`, dedup `ExternalOfferId`, `IsPrimaryNbo` = ordem motor, premissa 04 placeholder) + `NboInteractionService.approach(ExternalOfferId)` / `dismiss(...)` → update `Opportunity.NboStatus__c` + `Task` audit — **Depende de:** T-C02, T-C04, T-A04 — **Cenários:** 11–15 — **Aceite:** `approach` seta `Stage=Abordada` + `Task Subject=NBO Abordada`; `dismiss` seta `Dispensada` + `NboSuppressedUntil__c` na sessão (premissa 05).
- [ ] **T-C07 — CardService + ConsorcioService + InvestimentoService (lazy)** — `CardService.fetchVias(doc)→List<CardViaDTO>` (`GET NC_CartoesProcessadora/{doc}`, lazy só se `PRODUCT_SELECTED=CARTAO`), `ConsorcioService.fetchCotas(doc)→List<ConsorcioCotaDTO>` (`GET /consorcios/{doc}`, lazy só se `CONSORCIO`), `InvestimentoService.fetchPosicoes(doc)→List<InvestimentoPosicaoDTO>` (`GET /investimentos/{doc}`, lazy só se `INVESTIMENTOS`) — `LIMIT 50`, ordenação Ativo primeiro, badges funcionais — **Depende de:** T-C02, T-C04, T-A03 — **Cenários:** 17–20, EL-03/04/05 — **Aceite:** 4 vias (titular/adicional/ativo/cancelado/bloqueado) + 6 cotas (2 no mesmo grupo Rural) + 5 posições, conforme fixtures do protótipo; scroll sem `LimitException`.
- [ ] **T-C08 — CustomerSearchController (façade AuraEnabled)** — `CustomerSearchController` com `@AuraEnabled identify(String normalizedDoc, String modo) → CustomerSearchResultDTO` (valida via `DocumentValidator` conforme o modo PF/PJ/PROTOCOLO/NAO_CLIENTE, orquestra `CadastroService`+`NboService`+`HoldingService` em sequência server-side mas LWC faz `Promise.allSettled` com 2 chamadas separadas), `fetchVias(doc)`, `fetchCotas(doc)`, `fetchPosicoes(doc)`, `fixInteractionContext(...)`, `approachNbo`/`dismissNbo` — **Depende de:** T-C03–T-C07 — **Cenários:** 21 (paralelo) — **Aceite:** `identify('12345678909','PF')` retorna `CustomerSearchResultDTO` com `partialFaults` quando I02 timeout; sem DML síncrono.
- [ ] **T-C09 — CustomerUpsertQueueable + InteractionContextService** — `CustomerUpsertQueueable implements Queueable, Database.AllowsCallouts` (construtor recebe `CustomerSearchResultDTO`; `execute` faz `Database.upsert(accounts, Account.Document__c,false)`, `upsert(assets, Asset.ExternalContractId__c,false)`, `upsert(opportunities, Opportunity.ExternalOfferId__c,false)` + `EventBus.publish(new Customer360Upserted__e{DocumentNormalized__c, AccountId__c})`) + `InteractionContextService.build(...)→InteractionContextDTO` — **Depende de:** T-C08, T-A01–T-A04 — **Cenários:** RN-10, EL-11 — **Aceite:** 2 chamadas `identify` concorrentes mesmo doc não duplicam `Account` (External ID); `UNABLE_TO_LOCK_ROW` retry 1×; `Customer360Upserted__e` publicado.
- [ ] **T-C10 — AssetTriggerHandler esqueleto (bypass ativo)** — Classe `AssetTriggerHandler extends TriggerHandler` com `beforeInsert/beforeUpdate` só validando `Last4__c` regex + `ExternalContractId__c` required, guard `if(TriggerBypass__c.getInstance().Asset__c) return;` — **Depende de:** T-A07 — **Cenários:** — **Aceite:** `Asset__c=true` (default) handler não executa lógica além de validação; sem callout/DML em cadeia.

## Grupo D — UI LWC modular via LMS (100% customizado, sem OmniStudio)

Dependência: C (Apex pronto). Bloqueia F (testes de UI).

- [ ] **T-D01 — CustomerInteractionChannel__c (LMS)** — Criar `CustomerInteractionChannel__c.messageChannel-meta.xml` com `isExposed=true`, `description` mensagens: `CUSTOMER_SEARCH_TRIGGERED`, `CUSTOMER_IDENTIFIED`, `NBO_OFFERS_AVAILABLE`/`NBO_EMPTY`, `PRODUCT_SELECTED`, `CARD_SELECTED`, `INTERACTION_CONTEXT_FIXED`, `NBO_APPROACHED`/`NBO_DISMISSED` — **Depende de:** T-C08 — **Cenários:** P2, PT — **Aceite:** `lightning/messageService` `publish/subscribe` entre `c-customer-search-shell` e filhos sem `@api` pai→filho.
- [ ] **T-D02 — c-customer-search-shell (orquestrador)** — LWC `c-customer-search-shell` (shell/container): `Promise.allSettled([identifyCadastro(), identifyNbo()])` paralelas, skeletons independentes por área, publica `CUSTOMER_SEARCH_TRIGGERED` com `documentNormalized/documentMasked/docType`, lazy `fetchVias` — **Depende de:** T-D01, T-C08 — **Cenários:** P2, 21,22 — **Aceite:** I01 rápido + I02 lento → faixa/hub renderiza antes de NBO (Cen 21); timeout NBO não bloqueia hub (Cen 22).
- [ ] **T-D03 — c-customer-search-bar** — LWC `c-customer-search-bar`: seletor PF/PJ/Protocolo/Não cliente no topo, campo abaixo, botão "Localizar" abaixo do campo; máscara dinâmica CPF `000.000.000-00` / CNPJ `00.000.000/0000-00` / BACEN `00.AAA.000/0000-00` (12alfa+2dig, só PF/PJ), validação DV local, `isSearchDisabled` até formato válido (Protocolo 5+ chars; Não cliente qualquer valor), foco automático, `Enter`/`F2`, publica `CUSTOMER_SEARCH_TRIGGERED` — **Depende de:** T-D02, T-C03 — **Cenários:** 1–5, EL-01/02 — **Aceite:** `12345678999` mantém botão desabilitado (DV inválido EL-01); `12ABC34501DE35` habilita (BACEN).
- [ ] **T-D04 — c-customer-header-summary** — LWC `c-customer-header-summary`: cartão enriquecido (nome/razão, tipo, documento, segmento, situação do cadastro + blocos contato, endereço, renda/score, agência/conta), consome `CUSTOMER_IDENTIFIED` DTO em memória (não SOQL), skeleton `slds-skeleton` — **Depende de:** T-D02 — **Cenários:** 6 — **Aceite:** reflete `DemographicDTO` sem esperar `CustomerUpsertQueueable`.
- [ ] **T-D05 — c-product-hub + c-product-card** — LWC `c-product-hub` (grade) + `c-product-card` (1 por `ProductCatalog__mdt` ativo): renderiza `COUNT(Asset)` por tipo, `quantity/status`, estado selecionado reversível (`selected` class), publica `PRODUCT_SELECTED {productType}`, extensível sem código (add CMDT registro → novo card) — **Depende de:** T-D02, T-A06 — **Cenários:** 7,9,16, PT 23 — **Aceite:** sem `Asset` Porto Bank → empty state sem erro (Cen 9); add `FINANCIAMENTO_VEICULOS` CMDT → novo card sem deploy LWC (PT).
- [ ] **T-D06 — c-holding-strip** — LWC `c-holding-strip` (ou `variant="holding-informative"` em `c-product-hub`): lista `HoldingDTO` com ícone por produto, sem `PRODUCT_SELECTED`, distinguido visualmente, sem ação transacional — **Depende de:** T-D02 — **Cenários:** 8, RN-06 — **Aceite:** holdings do fixture aparecem com ícone e selo "somente leitura", sem botão `Selecionar`; premissa 02 placeholder sem filtro.
- [ ] **T-D07 — c-nbo-banner** — LWC `c-nbo-banner`: narrativa da oferta (produto, elegibilidade, bandeiras, limite pré-aprovado, anuidade, benefícios), consome `NBO_OFFERS_AVAILABLE`/`NBO_EMPTY`, skeleton próprio, colapsa se 0 ofertas (Cen 12), múltiplas → placeholder "Ver N ofertas" desabilitado (premissa 04), ações `Abordar venda` → `NBO_APPROACHED` (Cen 14) / `Dispensar` → `NBO_DISMISSED` (Cen 15) — **a decisão libera o hub (gate); sem ela o hub fica bloqueado com aviso** — **Depende de:** T-D02, T-C06 — **Cenários:** 11–15 — **Aceite:** sem ofertas não ocupa espaço e hub libera direto; com oferta, hub só libera após Abordar/Dispensar.
- [ ] **T-D08 — c-card-drawer + c-consorcio-drawer + c-investimento-drawer** — LWCs modais de sub-seleção em cards brancos matte com contorno pastel: `c-card-drawer` aberto por `PRODUCT_SELECTED {CARTAO}` (lazy `fetchVias`: brand/category/last4Masked/holder/isTitular/limite/anuidade/badge, publica `CARD_SELECTED`); `c-consorcio-drawer` por `{CONSORCIO}` (lazy cotas: tipo/grupo/cota/valor/contemplação/parcela/status, publica `COTA_SELECTED`); `c-investimento-drawer` por `{INVESTIMENTOS}` (lazy posições: classe/produto/valor/rentabilidade/vencimento/risco, publica `INVESTIMENTO_SELECTED`) — scroll/paginação `LIMIT 50` (EL-03), itens cancelados/bloqueados selecionáveis (EL-04/05), Cancelar desfaz a seleção → `INTERACTION_CONTEXT_FIXED` — **Depende de:** T-D02, T-C07 — **Cenários:** 17–20, EL-03/04/05 — **Aceite:** 4 vias + 6 cotas (2 no grupo Rural 9012) + 5 posições conforme fixtures; selecionar cancelada fixa `InteractionContextDTO` (Cen 19).
- [ ] **T-D09 — estados transversais P9** — Patterns `c-skeleton`/`c-inline-error`/`illustration` para P9 (loading por área, empty, partial, total) no shell — sem `c-recent-searches` (Cenário 24 removido) — **Depende de:** T-D02 — **Cenários:** 10,21,22, EL-08/09 — **Aceite:** não-encontrado preserva doc digitado; falha parcial NBO exibe hub normalmente; erro total preserva doc com retry.

## Grupo E — Migração progressiva (não bulk)

Dependência: A+C (upsert Queueable já existe). Não bloqueia D, mas teste F precisa dele.

- [ ] **T-E01 — Upsert progressivo idempotente (sem data-mapping.md bulk)** — Validar `CustomerUpsertQueueable` com `Database.upsert(..., ExternalId, false)` + `Customer360Upserted__e` Platform Event (campos `DocumentNormalized__c`, `AccountId__c`) — sem `data-mapping.md` nesta capacidade (bulk é `_fundacao/002`) — **Depende de:** T-C09 — **Cenários:** RN-10, EL-11 — **Aceite:** re-busca mesmo doc não duplica `Account/Asset/Opportunity`; `EventBus` entrega para `household-360/*`.

## Grupo F — Testes (Princípio IX: cada cenário tem teste)

Dependência: A+B+C+D+E. Bloqueia G.

- [ ] **T-F01 — Apex tests com HttpCalloutMock (75%+)** — Classes: `DocumentValidatorTest` (CPF DV/CNPJ DV/BACEN 12+2/formatos inválidos), `CadastroServiceTest`/`HoldingServiceTest` (mock I01 carteira 4 Porto Bank + 6 Holding), `NboServiceTest` (0/1/múltiplas ofertas, dedup), `CardServiceTest` (4 vias: titular/adicional/ativo/cancelado/bloqueado; scroll EL-03 via mock), `ConsorcioServiceTest` (6 cotas, 2 no mesmo grupo), `InvestimentoServiceTest` (5 posições), `CustomerSearchServiceTest` (notFound Cen10, allSettled Cen21), `CustomerSearchControllerTest` (timeout I02→`ApiFaultDTO` Cen22), `CustomerUpsertQueueableTest` (idempotência EL-11, `UNABLE_TO_LOCK_ROW` retry), `NboInteractionServiceTest`, `InteractionContextServiceTest`, `ProductCatalogServiceTest` (PT) — fixtures JSON em `staticresources` mock — **Depende de:** T-C01–T-C10 — **Cenários:** todos ativos — **Aceite:** `sf apex run test --test-level RunLocalTests` passa ≥75% coverage por classe; cada serviço tem `Test.setMock(HttpCalloutMock, new MockForIxx())`.
- [ ] **T-F02 — Jest LWC (sfdx-lwc-jest, LMS mock)** — `__tests__/` para: `c-customer-search-bar.test.js` (4 modos, mask só PF/PJ, Enter/F2, disabled, foco), `c-customer-search-shell.test.js` (orquestração, skeletons, gate NBO), `c-customer-header-summary.test.js` (card enriquecido), `c-product-hub.test.js` (empty PT), `c-holding-strip.test.js` (ícones, somente leitura), `c-nbo-banner.test.js` (narrativa, colapso, Abordar/Dispensar), `c-card-drawer.test.js` + `c-consorcio-drawer.test.js` + `c-investimento-drawer.test.js` (lazy, badges, cancelar desfaz seleção) — reusando seeds `prototype/data/buscaCliente/buscaCliente.js` — **Depende de:** T-D01–T-D09 — **Cenários:** todos ativos — **Aceite:** `npm run test:unit` passa; `lightning/messageService` mock cobre `publish/subscribe`.
- [ ] **T-F03 — Validação funcional 23 cenários (restore + preview)** — Checklist via overlay temporário (`node scripts/restore-prototype.mjs` + `npm run build` + `npm run open -- /busca-cliente`, limpar após): Cen1 CPF `123.456.789-00`, Cen2/3 PJ `12.ABC.345/0001-90`, Cen4 4 modos + formato inválido, Cen5 Enter/F2 + foco, Cen6 card enriquecido, Cen7 hub (4/6/5 itens), Cen8 holding com ícones, Cen9 `111.111.111-11` sem hub, Cen10 `000.000.000-00` não encontrado, Cen11 NBO Black + gate do hub, Cen12 PJ sem oferta (libera direto), Cen13 múltiplas (placeholder), Cen14 abordar, Cen15 dispensar libera hub, Cen16 conta digital direta, Cen17–20 modais cartão (4 vias) / consórcio (6 cotas, 2 no grupo 9012) / investimentos (5 posições) + Cancelar desfaz, Cen21 skeletons por área, Cen22 `999.999.999-99` parcial, Cen23 extensibilidade (inspeção), EL-08 `888.888.888-88` total com retry + ELs — **Depende de:** T-D09 — **Cenários:** 1–23 ativos + ELs (Cen24 removido) — **Aceite:** cada cenário tem linha navegável verificada (plan.md §8 + roteiro em `prototype/README.md`); gravar `evidence.md` com screenshots opcional.
- [ ] **T-F04 — Gate de qualidade (lint + deploy dry-run)** — Executar `sf project deploy preview --manifest package.xml` + `sfdx scanner:run` (PMD/ESLint LWC) + `experience-accessibility-validate` (WCAG AA baseline) — **Depende de:** T-F01–T-F03 — **Cenários:** premissa 14 — **Aceite:** 0 violation `High`, `package.xml` só contém `busca-cliente/001` + `_fundacao` share, não outro domínio.

## Grupo G — Cutover / Deploy (cross-domain isolado)

Dependência: F. Último.

- [ ] **T-G01 — package.xml + manifest de deploy isolado** — Criar `manifest/package-busca-cliente-001.xml` escopado a esta capacidade (`CustomObject` fields, `CustomMetadata` `ProductCatalog__mdt`, `PermissionSet` 3, `NamedCredential` 5, `ApexClass` 13, `LightningMessageChannel` 1, `LightningComponentBundle` 9: shell + search-bar + header + hub + holding + nbo-banner + 3 drawers) + `_fundacao` share, sem incluir `atendimento/*` ou `household-360/*` — **Depende de:** T-F04 — **Cenários:** — **Aceite:** `sf project deploy start --manifest ... --dry-run` sem erro; `sf project retrieve` round-trip limpo.
- [ ] **T-G02 — Ativação + smoke + rollback plan** — Atribuir `PS_BuscaCliente_Agente` a usuário piloto, inserir `Account.Document__c='12345678909'` fixture, executar `identify()` em sandbox com mocks `NC_*` apontando para mock server, verificar skeletons→hub→NBO→drawer, plano rollback = desativar `PS` + `TriggerBypass__c` — **Depende de:** T-G01 — **Cenários:** — **Aceite:** piloto consegue jornada completa em ≤2 gestos (RN-13) com `TMA` medido.

---

**Total tasks:** 36 (A:7 + B:3 + C:10 + D:9 + E:1 + F:4 + G:2) — ordenado por dependência real.
**Artefactos concretos nomeados:** 36 tasks → 50+ artefactos físicos (4 fields Account/Contact + 19 fields Asset + 9 fields Opportunity + 7 Product2 + 1 CMDT/7 registros + 3 VR/1 CS + 3 NC + 13 Apex classes/DTOs/PE + 1 LMS + 9 LWC bundles) — cada task nomeia artefacto concreto (campo External ID, CMDT, PS, NC, Apex class, LWC).
**Cobertura:** 23 cenários ativos spec.md (Cenário 24 removido) + 17 premissas abertas como placeholder neutro (nenhuma regra inventada) + ELs críticos.
